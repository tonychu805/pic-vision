# AGENTS.md

Agent-facing instructions for this project live in [`CLAUDE.md`](./CLAUDE.md) — read that file, not this one. Kept as a separate pointer for agent tools that look for `AGENTS.md` specifically, so there's one source of truth instead of two files drifting apart.
