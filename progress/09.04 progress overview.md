# 2026-09-04

**`track_ball` re-acquisition now checks a predicted position too, not just the last confirmed one — measured fix for real dropout-driven rally fragmentation (`DECISIONS.md` ADR-072, `EXPERIMENTS.md`).** Found on real footage: `brickwall-SEMI` rally_id 1 (a near-net dink exchange, `quality:1`) was fragmenting — short raw-detection dropouts (likely occlusion/small apparent size) meant the ball reappeared further from its last known spot than a flat `max_jump` re-check would accept, so the tracker lost it and the rally's lead-in got cut off. First attempt widened the acceptance radius by elapsed gap frames (tried flat/linear/sqrt/capped-linear/log2 scalings) — `sqrt` looked best on `brickwall-SEMI` alone, but checking it against the rest of the locked dev set caught a real regression: it silently dropped a genuine `quality:1` rally on `pb_draft_cup_30fps` (rally 9), because a wider radius can't tell "the ball, further along its path" from "background clutter merely within range." Rejected that approach and shipped a different one instead: accept a candidate near *either* the last confirmed position or a velocity-predicted one (two-point estimate, no smoothing) — direction, not just distance, is what actually separates a re-acquired real ball from clutter. Verified against the full locked dev set plus the specific `pb_draft_cup_30fps` rallies the rejected approach broke: no regressions, one net gain on `IMG_7744`. `brickwall-SEMI` rally_id 1 itself improved (its best-matching candidate now covers 14.6s of its 20s label window, up from 9.8s) but isn't fully recovered — real, partial progress on `PIC-33`, tracked separately from the label lead-in issue (`PIC-55`). Committed `3866f28`.

**Also: chopping the two newest raw sessions (`IMG_7893.MOV`, `IMG_7894.MOV`) into per-segment clips around camera-drift bumps found by `check_drift.py`, per `ADR-049`'s standing rule (a calibration is invalid past a bump — split and calibrate each stable stretch separately).** `IMG_7893` is fully segmented into 7 stable-window clips (skipping the bump regions between them, and the badly-drifting tail past 2490s — not yet checked by playback, still open). `IMG_7894`'s 1470s–2095s tail looked ambiguous from the drift log alone (erratic, low-confidence `resp` readings rather than one clean bump) — resolved by actual playback, not stills, per the project's standing rule: operator watched three real clips from that stretch. 1470–1500s and 1770–1800s are genuine rally/deadball play, no camera movement. 2060–2095s is the camera lens covered by a cloth (operator confirmed by eye) — end-of-session teardown, not usable. Pinpointed the actual transition frame-by-frame: clean through ~1858s, then the same obstruction creeps in and never clears. `videos/IMG_7894_1470-1855s.mp4` cut accordingly; the remaining 1855–2095s (~240s, cloth-obstructed) deliberately left uncut. `IMG_7894` segmentation is now done: `150–570s, 570–750s, 750–1050s, 1050–1470s, 1470–1855s` (1705 of 2096s kept).

**`IMG_7893`'s tail past 2490s (already excluded from the morning's segmentation) checked the same way and confirmed correct — this one wasn't a stable second segment at all.** The stills showed something different from `IMG_7894`'s steady cloth-drape: normal court frames mixed with close-up fingers and a swinging cord, ending in a black frame. Operator watched the real clip (2505s to end-of-file) and confirmed: someone unmounting the camera, end of session. Matches the chaotic (not single-bump) drift-log swings from ~2520s on. No further cut needed — `IMG_7893`'s existing 7-segment split (`090–390s` through `2130–2490s`) already stops in the right place. Both videos' segmentation is now settled: `IMG_7893` (7 clips, 090–2490s) and `IMG_7894` (5 clips, 150–1855s). Neither has a calib file yet — that's the next step.

**Calibrated all 7 `IMG_7893` clips separately (per `ADR-049` — each stable stretch between bumps needs its own calibration, since the camera may sit differently after each bump even if drift within a segment is fine).** Used `calibrate.py`'s interactive 12-court-point + 2-net-point click flow for each clip, picking an unobstructed frame per clip first (scanned each clip for a moment with clear sightlines to all corners/NVZ lines before launching). RMSE results: `090-390s` 0.429 ft, `390-690s` 0.677 ft, `690-930s` 0.709 ft, `930-1110s` 0.462 ft, `1230-1590s` 0.617 ft, `1830-2130s` 0.820 ft, `2130-2490s` 1.143 ft. The first attempt on `090-390s` came back at 1.344 ft with one point 3.4 ft off (a clear misclick, not distortion) — redone with a better frame and it dropped to 0.429 ft. The rest all had one point elevated (1.1–2.0 ft) against an RMSE at or near the project's known ~0.8 ft lens-distortion floor (`project_camera_lens_distortion` memory) — operator reviewed each and chose to accept rather than redo (consistent with the precedent of accepting `IMG_7743_calib.json` as-is). `IMG_7894` still needs calibration, per clip, the same way. All 7 `IMG_7893` calib files committed to `calib/`.

**Per-clip drift check (`check_drift.py`, `--step-sec 5`) on all 7 `IMG_7893` clips found real problems the morning's coarse 10s full-file scan missed.** `090-390s` and `390-690s` each have a ~25-30s unstable settling period right at their own start (the morning's cut boundaries landed mid-transition, not after it) — their calibration frames (t=120s, t=80s) are safely past that, so no fix needed there. `930-1110s` has a small bump (+6.5px) only in its last ~20s; `1830-2130s` has one early (t=5-10s) that its calibration frame (t=40s) is already on the correct side of, plus a further rise in its last ~40s. `1230-1590s` and `2130-2490s` are flagged CREEP (continuous wander, no clean split point) rather than a discrete bump — a known, unfixable-by-recalibration limitation per `check_drift.py`'s own docs, not a new problem.

**`690-930s` was worse: its calibration was actually wrong.** A real bump at t=25-30s (+20px dy) landed the click frame (t=20s, chosen before this check existed) on the wrong side — the stable plateau after the bump covers ~80% of the clip, not the ~20s before it. It also has a second, more severe disturbance at t=220-235s (dy swinging to -88px, confidence collapsing to 0.04-0.12) matching the same anomaly the original full-file scan flagged around absolute t=910-940s. Started a recalibration at t=100s (safely in the stable plateau, clean sightlines) but operator chose to drop the clip entirely instead — `videos/IMG_7893_690-930s.mp4` and its calib file removed. `IMG_7893` usable footage is now `090-690s` (clips 1-2), a gap, then `930-2490s` (clips 4-7) — 6 clips, not 7.

**Ran TrackNet (k14) inference on all 6 remaining `IMG_7893` clips, local GPU (RTX 2000 Ada, `/mnt/fast_scratch/tf215_env/venv`, `scripts/pod_infer.py --calib` for court masking).** ~65-69 fps throughout, ~2.1x realtime, 100% of frames decoded on every clip (no corruption). Predictions cached at `cache/IMG_7893_<range>_predictions_k14.csv` (gitignored, regenerable). Next step in the pipeline: rally detection (`track_ball` → `crossing_times` → `cluster_crossings`) to turn these into candidate rally segments.

**Ran rally detection on all 6 clips** (`rally_segments_from_predictions`, shipped defaults `gap_sec=3.0`/`min_crossings=6`) — 35 candidate segments total, cached at `cache/IMG_7893_<range>_rallies_k14.json` (gitignored).

**Started ground-truth labeling (`label_web.py`) — in progress, not finished.** Operator normally remotes into this desktop via Thincast (explains why the earlier `calibrate.py`/`ffplay` desktop-window approach worked smoothly — same X11 session); tried `label_web.py`'s separate browser-over-Tailscale path instead this time, which stacked a second network hop on top of Thincast and caused laggy video. 3 of 6 clips got full label passes before that friction — `eval/labels/IMG_7893_090-390s.jsonl` (13 rallies), `IMG_7893_390-690s.jsonl` (13), `IMG_7893_1230-1590s.jsonl` (11) — **not yet committed**, and validation caught real problems: `090-390s` has one small overlapping-boundary pair (rally 9/10), `390-690s` has two (rally 3/4, rally 7/8) — likely lag-induced boundary mistakes, not reviewed by playback yet (3 clips sent to operator as files, verdict pending). `1230-1590s` is clean (no overlaps). `930-1110s`, `1830-2130s`, `2130-2490s` not started. Operator is deferring the rest until back at this machine locally — resume with `label.py`'s desktop window (not `label_web.py`) next time, per the Thincast realization above.

**Trued up stale Linear ticket statuses for the desktop/cloud-console work.** `PIC-76` (sample-clip camera source), `PIC-68` (cloud_pipeline wiring), `PIC-75` (live-camera calibration) were all still "In Progress" despite being fully built 2026-09-02/03 (verified against the actual code, not just memory — `addCameraFromSampleClip`, `cloud_pipeline`/`run_desktop_job.py` calls, `grabSnapshot`/`calibration.js` all present) — marked Done. `PIC-73` (the ADR-071 cloud-web-app tracking issue) is a different case: real progress has landed since filing (Supabase DB/auth, pairing+heartbeat, camera sync, all 8 mockup screens ported) but its core asks — a cloud→agent command channel (today's connectivity is agent→cloud reporting only) and the Schedule-feature migration — are still genuinely open, so moved to In Progress with a comment rather than closed.
