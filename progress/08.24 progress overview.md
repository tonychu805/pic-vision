# 2026-08-24

## First end-to-end pipeline run on brand-new footage, and a real "is this broadcast footage" scare that turned out fine

Operator handed over a genuinely new video — `Brick-Wall--Mid-Atlantic.mp4` — and asked for the full pipeline run start to finish, ending in a highlight reel. Different from yesterday's `brickwall-SEMI` work: no hand labels exist for this one, so this tested the *operational* pipeline (does it run cleanly, does the output look right), not detection accuracy.

**Real scare, resolved carefully rather than assumed away.** The file's provenance (a matching download in `~/Downloads` named `YTDown.com_YouTube_Brick-Wall-Pickleball-Mid-Atlantic...`) pointed straight at YouTube — and this project already archived two YouTube broadcast videos as unusable (19–25 hard scene cuts per 3 minutes, `progress/08.18 progress overview.md`). `check_drift.py` alone wasn't trusted as sufficient to clear it (its averaging could plausibly mask many cuts that happen to cancel out) — pulled frames at four points spread across the 21-minute video and confirmed identical framing throughout, including the same tripod-mounted phone visible in every shot. Genuinely a single fixed-camera recording someone uploaded to YouTube, not an edited broadcast. Worth remembering: **file origin (YouTube, an unusual filename) is a legitimate reason to double-check even when the automated drift tool says stable** — it wasn't built with hard-cut detection specifically in mind.

**Pipeline ran clean end to end:** decode integrity checks (before and after), 25→30fps CFR conversion, operator-clicked calibration (0.283ft RMSE — calibration stayed a human-in-the-loop step, per `STRATEGY.md`'s explicit "never blind auto" stance), local-GPU TrackNet inference (~24 min), detection (56 candidates), ranking (`src/select.py`'s locked ADR-063 formula), and a 12-clip, ~4:58 reel — both chronological and rank-ordered versions.

**One real bug, caught by checking rather than assuming:** generalizing `scripts/rank_and_reel.py` for reuse (previously had `brickwall-SEMI` hardcoded) silently dropped the call that builds the chronological `highlight.mp4` — only the ranked version was actually written. Found by verifying both output files exist before calling it done, not by trusting the script ran correctly. Fixed and reverified.

**Reviewed by frame sample, not yet full playback.** Pulled one mid-clip frame per clip (all 12) plus both boundaries of the top-ranked clip — every clip shows genuine active rally play, boundaries land on natural pre-serve/post-point moments. Explicitly **not** a final verdict — this project's own rule is that stills-based verdicts have been wrong before (`IMG_7744` FP review). Full playback review, especially of the shortest/lowest-ranked clip that made the cut (#12, 7.1s), is the next thing to actually do, not done yet.

**Session ended here — operator going offline.** Nothing scored, nothing labeled for this video yet. Uncommitted: `calib/brickwall_mid_atlantic_calib.json` (should be committed per this project's convention — calibrations are expensive to redo) and the `rank_and_reel.py` bugfix. Asked twice about committing, no confirmation came before the session ended — **do not commit without being asked next time either**, just because it "should" be committed by convention.

## Update — a real coordinate bug, found by building a diagnostic for an unrelated reason

Asked to visualize every signal the pipeline uses "on a video" — built an actual annotated clip (court-wedge boundary, net line, tracked ball, crossing flashes, spike rings burned into the footage), not just a static-frame chart. Watching it, the operator caught something real: the tracked marker sat next to the ball, not on it.

**Root cause, confirmed with exact-match math before trusting the fix:** `scripts/pod_infer.py` scales TrackNet's predictions back to source pixels using one height-derived ratio for both x and y, but the frame preprocessing squashes to a fixed 512×288 without preserving aspect ratio. Only correct on exactly-16:9 video — every prior video in this project happens to be 16:9; `brickwall_mid_atlantic` (1280×640, 2:1) is the first that isn't, which is exactly why this had never surfaced before. Fixed (separate x/y ratios), re-ran inference, and *measured* the actual shift rather than trusting the math alone: 142px max, correlating 1.000 with the old x-position — a clean, fully-confirmed linear scale error.

**Re-ran detection and the reel on the corrected data, comparing candidate-for-candidate rather than just counts.** 51 of 56 candidates and 10 of 12 reel clips unchanged; the handful that differ all trace cleanly back to crossings near the court-wedge's x-boundary flipping in/out — one false candidate that only existed because of the bug disappeared, one real one split after losing two bug-only crossings. Both replacement reel clips frame-verified as genuine before accepting the swap. Full detail: `EXPERIMENTS.md` and `DECISIONS.md` ADR-064.

**Worth remembering:** this bug would have silently corrupted `court_wedge` gating on any future non-16:9 footage indefinitely — found through ordinary use of a diagnostic built for a different purpose, not a targeted audit. Net-crossing detection itself was never affected (y-only), so no prior video's results need re-checking.

**Next session, in order:**
1. Full playback review of the *corrected* reel (`clips/brickwall_mid_atlantic_reel_5min_fixed/`), not just frame samples.
2. Decide whether this video gets labeled at all (it has no ground truth yet — would need a presence pass per `LABELING.md` if the operator wants a real precision/recall number on it, same as `brickwall-SEMI` got).
3. Still outstanding from yesterday: rally 29's unresolved 3-way conflict on `brickwall-SEMI` (`EXPERIMENTS.md`, 2026-08-23), and validating `rank_segments`'s ranking formula against real `quality:1`/`quality:2` grades.

## Update — the signal-overlay tool got saved for real this time, and watchable as a Claude Artifact

The diagnostic clip that caught the coordinate bug above was thrown together once and never kept — gone by the time this conversation started fresh. Rebuilt it as an actual reusable script, `scripts/visualize_signals.py` (still uncommitted), that burns the court gate, net line, tracked ball, velocity spikes, net crossings, and rally-segment membership into real footage on request, instead of a one-off inline job. Ran it on the same top-ranked `brickwall_mid_atlantic` clip and published it as a Claude Artifact ("Rally Signal Viewer") so it's directly watchable in a browser with a legend, not just a local file to hunt down.

**Also added the PIC-31 pre-serve stillness signal (`scripts/pose_stillness.py`) to the same viewer, clearly marked as experimental/not-shipped** — an ankle-speed meter plus the computed ratio at the clip's rally-start boundary. Came out to 0.60 on this boundary: a real dip, but milder than the original 7–17% sample. Doesn't change anything — it's one more data point behind the existing conclusion (stillness is a loose leading indicator, not a precise boundary marker, per PIC-53) — but worth having since it came from a third video almost for free.

**Clarified for the record:** a low stillness ratio is *not* a rule that defines a rally start. It's correlational (dips tend to precede real starts, 88% on the one video tested), doesn't localize the boundary precisely, and — critically — has never been validated against real dead time, since every dead-time example this project had turned out on playback review to be real plays (ADR-057). "Low ratio → rally start" is an overclaim the data doesn't support yet.

**Still uncommitted:** `scripts/visualize_signals.py` (new file). Not committed — wasn't asked to.

## Update — first ranked highlight reel produced for IMG_7743

Asked to produce a highlight on `IMG_7743`. `scripts/rank_and_reel.py` (used for `brickwall_mid_atlantic` above) only takes a single `--calib`, which doesn't work here — `IMG_7743` has a mid-session camera bump (ADR-049) requiring separate calibration for [0, 2900s) vs [2900s, end). Wrote `scripts/rank_and_reel_split.py`: same ranking/cutting pipeline, but takes multiple `--calib PATH T_MIN T_MAX` pieces, scores each range against the existing cached `cache/IMG_7743_predictions_k14.csv` (no re-inference needed) with its own calibration, then merges everything before ranking so scores are comparable across the full session.

**Checked against a number already on record before trusting the output:** 69 prebump + 22 postbump = 91 total candidates, exactly matching the split-calibration segment count already reported in `EXPERIMENTS.md`'s 2026-08-23 entry — good sign this is equivalent to the project's established split-calibration method, not a new computation that happens to look plausible.

Cut a 5-minute, 17-clip reel: `clips/IMG_7743_reel_5min/highlight.mp4` (chronological) and `highlight_by_rank.mp4` (best-first). Top clip is 863.5–887.9s, one of the two long segments PIC-37 flagged as plausibly-real-not-dead-time back on 2026-08-23 — ranking it #1 is consistent with that flag, not an independent confirmation of it.

**Not done: full playback review.** Same open item as `brickwall_mid_atlantic`'s reel above — a candidate-count match is a sanity check, not a verdict on whether these 17 clips are actually the right highlights. `TECH_SPEC.md` §12 updated in the same change to list the new script. Nothing committed yet — new file (`scripts/rank_and_reel_split.py`) plus doc edits (`TECH_SPEC.md`, `EXPERIMENTS.md`, this file) are uncommitted, matching this session's pattern of not committing without being asked.

## Update — `brickwall_pro_series_finals.mp4` turned out to be footage the project already has

Asked to produce a highlight on `brickwall_pro_series_finals.mp4` (`videos/raw/`). Checked its provenance before touching it, same instinct as this morning's YouTube scare — duration (1513.24s) and resolution matched `videos/brickwall_30fps.mp4` almost exactly, and a side-by-side frame pull at t=500s showed the identical scene. Turned out `EXPERIMENTS.md` already says so directly: this raw file **is** what `brickwall_30fps.mp4` was 60→30fps-converted from, back when this video was originally calibrated, hand-labeled (33 rallies, quality-graded), and scored. Not new content — flagged this before doing anything expensive with it.

**Operator's call after being asked: build a reel anyway, with fresh TrackNet inference but reusing the existing calibration** (recalibrating needs an interactive click session that can't happen without the operator in this session). Ran inference on the existing CFR file (re-deriving that conversion would just reproduce it) — 27.7 min on the local GPU, 45,396/45,397 frames, output kept as a separate file from the already-cached predictions so nothing existing got overwritten. Ranked and cut a 5-minute, 8-clip reel: `clips/brickwall_pro_series_finals_reel_5min/`.

**Not done:** full playback review (same standing item as the other two reels today), and checking whether this fresh inference run actually reproduces the same detections as the existing cached run on the same footage — TrackNet isn't guaranteed deterministic run-to-run, worth knowing but not what was asked for here.

## Update — two confirmed dead ends (`IMG_7652`, `IMG_7655`), a quick win (`IMG_7744`), a deferred real gap (corner-angle footage), and the ranking formula finally validated

**`IMG_7652` and `IMG_7655`, both explored and both hit real walls** — full detail in `EXPERIMENTS.md`'s "two confirmed dead ends" entry. `7652` is genuinely continuous non-fixed-camera drift (ran the full pipeline anyway at the operator's request: 1 candidate out of ~13 minutes of real play). `7655` looked more promising at first — a real single bump like `IMG_7743`'s, not continuous drift — but the camera is mounted at/near the near baseline, so the near-court calibration points are out of frame in every sampled timestamp across both bump-split segments. That's very likely the answer to a 6-week-old unexplained gap already on record ("IMG_7655 has 36 labels but no calibration"). Operator's call: drop both, no further work without new partial-calibration tooling.

**`IMG_7744`: the fast case.** Already had calibration and a full-video k14 prediction CSV cached from Aug 17 (part of this week's four validated videos) — just ran detection + ranking + reel-cutting, no calibration or inference needed. 23 clips, 4:56.6, top clip 261.7–269.5s. Confirmed the old cached CSV is still valid post-`ADR-064`: `IMG_7744` is exactly 1920×1080 (16:9), so the coordinate bug was a mathematical no-op on it regardless of when it was generated.

**A genuine corner-angle video showed up** (`Mike-Bill-vs-George-Joshua-at-Rockland.mp4`) — confirmed by frame inspection that the net line has real slope (~20–35px between posts), exactly the gap `src/ball.py`'s own docstring already admits (behind-baseline only). Designed the fix (interpolate net height by image-x between the two marked net points, instead of `net_line_y`'s current flat average) but didn't build it — operator deferred it explicitly ("save it for another day"). Memory note left so this doesn't need rediscovering.

**Ranking formula validated against real quality grades, closing `ADR-063`'s explicit open item.** `scripts/validate_ranking.py`: ran the shipped detector + `rank_segments` on all four graded videos, matched at IoU≥0.5. **The combined score is higher for `quality:1` than `quality:2` on all four videos, no exceptions** — `duration` is the strongest single component (perfect 4/4), `peak_crossing_rate`/`n_spikes` each right on 3 of 4 alone, but the combined score holding everywhere is the actual point of combining signals. No weight changes — this confirms the existing formula. `ADR-063` updated in place with this result.

**Committed and pushed this update** (docs + `scripts/validate_ranking.py`), per the operator's explicit ask this time.
