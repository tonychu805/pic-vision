# 2026-08-25

## Linear cleanup pass, then built PIC-27

Continuation of 2026-08-24's session (date rolled over mid-session). After finishing that day's pipeline/footage work, did a full pass through Linear's `Rally Detection Accuracy` project at the operator's request — 45 issues reviewed, not just skimmed.

**Closed (5):** `PIC-56` (brickwall-SEMI validation, done), `PIC-7` (highlight-worthy signal, answered with a validated ranking model), `PIC-5` (calibrate IMG_7655, definitively impossible — camera framing), `PIC-36` (pb_draft_cup's "7 junk segments," premise superseded by the 2026-08-23 relabel), `PIC-21` (stale status mismatch — description already said resolved, the status field never caught up; also flagged a real gap the "resolved" note glossed over — one of the three external videos was quietly dropped, not fixed). `PIC-45` also closed at the operator's explicit call ("way past that") — the definition half was already done in `LABELING.md`, the consistency-check half superseded by `PIC-7`'s validation.

**Deprioritized to Low, not closed** (real problems, just nothing to work on right now): `PIC-31`/`PIC-34` (no confirmed dead-time material left anywhere in the project to test against), `PIC-4` (height-cap bug confirmed real but not affecting any currently-active video), `PIC-23` (camera-angle question answered, fix designed, nothing urgent needs it), `PIC-48` (kitchen double-crossing — the one place it mattered, ranking, already sidesteps it), `PIC-35` (recall-by-grade need already served ad hoc every time it's come up).

**Upgraded Medium → High:** `PIC-27` — two other open items (`PIC-23`, `PIC-48`) both turned out to depend on it.

**Then built PIC-27.** `track_ball`'s tracked x (`xout`) was already computed internally with the exact same teleport-rejection/confirmation/segment-pruning as y — it just never left the function. Added `return_x=False` (opt-in, default off, so every existing caller's return shape is unchanged — checked every call site to confirm none pass it). Segment pruning re-syncs the x track to the pruned y track so a dropped segment can't leave a dangling x with no matching y. 4 new tests, plus a real-data smoke test on `brickwall_mid_atlantic` (9,642 tracked frames, x/y in exact lockstep, y byte-identical whether `return_x` is set or not). Committed and pushed (`66fc77f`), issue closed.

**Not done:** the two things this unblocks (`PIC-23`'s corner-angle net-line fix, `PIC-48`'s trajectory-shape check) are still just unblocked, not built.

## Update — full playback review confirmed, closing the last standing open item from the prior session

Operator watched the five reels directly (not routed through this session's tools) and confirmed them good: "the reels produced are great." This closes the "not yet done: full playback review" item that had been carried on every reel built 2026-08-24 (`brickwall_mid_atlantic`, `IMG_7743`, `brickwall_pro_series_finals`, `IMG_7744`) — until now only frame-spot-checked, never confirmed at playback speed, per this project's own standing rule that stills-based verdicts have been wrong before. `PROGRESS.md` updated to reflect this as done rather than pending.

Also discussed, not started: whether it's time to explore production infrastructure (cloud storage/compute) now that detection+ranking are validated and playback-confirmed. Recommended holding off starting that as a side effect of "nothing urgent is queued" — `STRATEGY.md` frames venue/production deployment as a deliberately separate decision ("a different product, not a port"), not the automatic next step once the bug queue is quiet.

## Update — web UI so the operator can run a session without a terminal

Operator asked for a browser front end: upload footage, calibrate, let the pipeline run, preview and download the reel. Scoped explicitly to desktop/laptop only (no mobile/touch work) and purely additive — every existing CLI tool keeps working standalone.

Built `webapp.py`/`webapp/` (Flask): four pages (upload, calibrate, status, preview/download), with `webapp/pipeline.py` running each job through drift check → CFR convert → TrackNet inference (subprocess into the separate TF2.15 env) → `build_reel()` in a background thread, writing progress the status page polls. The calibration page reuses `calibrate.py`'s `compute_calibration`/`solve_assignment` directly rather than reimplementing them.

One real refactor landed as a prerequisite: extracted `build_reel()` out of `scripts/rank_and_reel.py`'s `main()` so the web app and the CLI share one detect/rank/reel implementation instead of two copies that could drift. Landed as its own isolated commit, verified byte-identical against the pre-refactor CLI output (same stdout, same `manifest.json`, same clip md5s) before anything else was built on top of it — caught one real bug in the process (the ranked table printed to stdout in the original, not stderr like everything else; the first extraction attempt collapsed that split, fixed and re-verified).

Verified end-to-end against a real 1280×640 clip (non-16:9 — the exact case `ADR-064`'s coordinate fix covers) through all four pages, including live status polling and video seeking. Full test suite green (130 passed). Tracked as `PIC-57`, closed. Committed `7751f85` (refactor) and `9978b5b` (web app) — not yet pushed to origin.
