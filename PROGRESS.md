# Progress Log

This file is a pointer, not the log itself — see [`AGENTS.md`](./AGENTS.md) for the same pattern applied to agent instructions. The actual history lives in [`progress/`](./progress/), one file per day, named `MM.DD progress overview.md`; sorted by filename, oldest first, so the last file in a directory listing is always the most recent. Restructured 2026-08-21 (documentation-maintenance review) — the old single growing file had accumulated multiple nested "superseded by the above" layers, which is exactly the reading-habit cost per-day files are meant to fix. Git history has the code-level detail; `progress/` is the narrative map. Metric-producing runs go in [`EXPERIMENTS.md`](./EXPERIMENTS.md); durable decisions in [`DECISIONS.md`](./DECISIONS.md). Doc-authority map (which file governs what) lives in [`CLAUDE.md`](./CLAUDE.md).

## ▶ NEXT SESSION — start here

**[`progress/09.04 progress overview.md`](./progress/09.04%20progress%20overview.md)** is the most recent entry as of this writing. Check the `progress/` folder for anything newer before trusting that.

**2026-09-04: cloud console's Courts tab wired real then merged into Cameras; `PIC-71`'s credential-scoping design locked in (`DECISIONS.md` ADR-073); and the whole reel pipeline moved cloud-side (`DECISIONS.md` ADR-074) — cut on the RunPod pod from a 1080p proxy instead of locally from full-res, with a real Reels tab reading the result.** Filed `PIC-78` for an internal multi-venue operator tool ADR-073 surfaced as a separate need. R2 storage cleaned up (8.9GB→0.135GB, was near its 10GB free-tier limit). **Ran the real, billed RunPod test — full success**, the first time this pod path has ever run for real (pod create/install/ffmpeg/inference/cutting/upload/terminate all worked first try; downloaded and confirmed the actual output is real, correct pickleball footage). ADR-074 is now fully verified against real infrastructure. Reels are now ranked-only end to end (chronological dropped entirely, operator request). **Schedule migrated from `desktop/` to the cloud console entirely** (`ADR-071`/`PIC-73`) — removed from the desktop app, not kept in both places; still not wired to actually trigger a recording (`PIC-72`, separately blocked on the still-missing cloud→agent command channel). See `progress/09.04 progress overview.md`.

**2026-09-04: `track_ball` re-acquisition now checks a predicted position too, not just the last confirmed one (`DECISIONS.md` ADR-072).** Fixes a real dropout-driven rally-fragmentation mechanism found on `brickwall-SEMI` rally_id 1; a radius-widening alternative was tried first and rejected after it silently dropped a real rally elsewhere in the dev set. Also: chopping new raw footage (`IMG_7893`/`IMG_7894`) into per-segment clips around camera-drift bumps — both now fully segmented after real playback checks. `IMG_7894`'s tail had a cloth-covered lens past ~1858s, cut around it; `IMG_7893`'s tail past 2490s turned out to be someone unmounting the camera, correctly excluded already. **All 7 `IMG_7893` clips calibrated, then per-clip drift-checked** (`check_drift.py`) — caught a genuinely wrong calibration on `690-930s` (calibrated on the wrong side of an internal bump, plus a second severe disturbance near its tail); operator chose to drop that clip rather than re-fix it. `IMG_7893` is now 6 usable clips (`090-690s`, gap, `930-2490s`), all correctly calibrated. **TrackNet inference (local GPU, k14 weights) run on all 6 clips** — 100% frames decoded, ~65-69fps — then rally detection (35 candidate segments). Ground-truth labeling started (3/6 clips, `label_web.py`) but paused mid-way — laggy over a redundant network path (operator normally remotes in via Thincast; `label_web.py`'s separate browser-over-Tailscale path stacks a second hop on top of that). 2 clips have overlapping-boundary labels needing a playback check before trusting them. Operator deferring the rest to next local session — resume with `label.py`'s desktop window instead. `IMG_7894` still needs calibration. See `progress/09.04 progress overview.md`.

**2026-09-03: a "sample clip" camera source (`PIC-76`) lets calibration/the cloud pipeline be tested by uploading a video file, instead of needing a real, court-facing camera.** Direct follow-up to the incident below -- `ManualAddDialog`'s "Add" flow now offers "a live camera" or "a sample clip"; the latter uploads a local file (`store.js`'s new `addCameraFromSampleClip`) and plugs into the existing recording-list/cloud-pipeline/calibration UI with minimal special-casing (calibration grabs its snapshot by seeking into the file via `ffmpeg -ss` instead of a live RTSP pull). Verified against a real *synthetic* test clip (`ffmpeg testsrc`, not real footage). **Surfaced a second related privacy gap while verifying it:** a leftover live snapshot from the real "Court 1" camera was found sitting in `/tmp` (its calibration modal had evidently been closed without Save or Cancel running) -- deleted immediately, and fixed structurally: `capture.js` now sweeps up any outstanding snapshot on app quit, and the calibration modal discards its own snapshot on unmount, not only via its buttons.

**2026-09-03: the desktop client's calibration control now takes a live snapshot and lets the operator click the court points, instead of only accepting an existing `calib.json` file (`PIC-75`, supersedes part of `PIC-68`).** Operator pushback on the file-picker-only flow PIC-68 shipped the day before. `capture.js`'s new `grabSnapshot` pulls one still frame from the camera's live stream; `cloud_pipeline/save_calibration.py` (new) reuses `calibrate.py`'s own homography-fit math as a subprocess rather than porting it to JS (verified byte-for-byte against a real prior calibration's RMSE); `CameraDetailPage.jsx`'s new `CalibrationModal` collects the 12 court + 2 net-tape clicks in-app, mirroring `webapp/templates/calibrate.html`'s browser flow. The old file picker survives as a secondary "Import file…" option. **A real mistake happened while verifying this:** a live snapshot was pulled from the real "Court 1" camera to test the mechanism, without checking what it was pointed at first — it was aimed at the operator's bedroom, not a court, and the frame briefly reached the conversation before being caught and deleted. Recorded as a standing rule going forward: verify camera-facing code against a synthetic image, never a live grab from a real configured camera. **Not verified:** the click-modal UI against a real snapshot of an actual court — neither camera on this machine is currently court-facing.

**2026-09-02: architecture split locked in — a thin local agent vs. a cloud web app (`DECISIONS.md` ADR-071, `STRATEGY.md` §5 rewritten to match).** Operator: "I was actually expecting heavy lifting on the web app (including camera management, etc)... schedulers, other court management tasks remain on the web app on the cloud." Real architecture conversation, not a feature request — worked through the forcing constraint first (ONVIF discovery is UDP multicast, a venue's cameras sit on a private LAN with no port-forwarding, so camera-facing work can only run on a machine at the venue, no matter what), then confirmed `webapp/` today is *not* a hosted multi-venue web app at all (a single-operator local Flask tool), so "the web app" is close to entirely unbuilt. **Decided:** local agent keeps camera discovery/management, recording, court calibration, transcoding, uploading, and cutting the final reel from local full-res footage (privacy-driven, ADR-043 — that file never leaves the venue); cloud web app takes scheduling, court/venue management, footage review, multi-venue admin, delivery to players. Connectivity is outbound-only from the local agent (no inbound port needed on the venue's router). Today's `desktop/` Schedule feature is explicitly interim — real and useful now, the reference design to port, but migrates wholesale to the web app later rather than living in both places. Nothing in this ADR is built yet — it's the target shape for `PIC-68`/`PIC-72` and the not-yet-created web-app work.

**2026-09-02: the desktop client can now actually pull a live stream — real manual recording built (`electron/capture.js`, closes the core of `PIC-66`).** A "Start recording"/"Stop recording" button on a camera's detail page spawns `ffmpeg` and records the camera's real RTSP stream to local `.mkv` segments, per `TECH_SPEC.md` §1.2's spec (RTSP pull, `-c copy`, 10-minute segments, wallclock timestamps, clean `SIGINT`-only stop including on app quit). Two real bugs found and fixed by actually running it against real cameras, not by trusting the spec: the spec's own `.mp4` example silently fails on `pcm_alaw` audio (which the Tapo C200 streams) — MP4 has no codec tag for it, fixed by using `.mkv`; and a fast `ffmpeg` failure used to report false success before reverting seconds later with no explanation — fixed with a 2-second startup grace period that surfaces the real error instead. Verified end-to-end against both real cameras (different audio codecs each, confirming the fix isn't overfit), including that a recording survives navigating away from that camera's page. **Not done:** tying this to the Schedule page's booked sessions (still a manual button only), CFR conversion, cloud upload, cloud-output cut-back, CDN delivery — the rest of `PIC-66`-`71`.

**2026-09-01: the desktop client (`STRATEGY.md` §5) is under way — `desktop/`, Electron + React — and the BC510 that blocked all day is now actually added and streaming.** Camera discovery/management is real and working: ONVIF WS-Discovery, an RTSP network sweep with a real protocol-level confirm step, and a two-rung RTSP fallback ladder (`electron/cameras/rtspProbe.js`) for cameras whose ONVIF doesn't work at all — automatic common-path retry, then a raw-RTSP-URL last resort — built after discovering the target camera's ONVIF was switched off (a Synology-specific "Operation Mode" setting) but its actual video stream worked the entire time. The full 5-page UI was implemented from a Claude Design mockup handoff, with a full plain-language copy pass done as a non-technical venue owner. Several real bugs found and fixed, more than one corrected after an initial wrong guess (an Electron preload ESM/CJS mismatch, a WS-Discovery false-positive, an RTSP Digest-auth nonce bug caught by testing against the real camera rather than trusting the math). Explicit product direction set: stay ONVIF-generic, no vendor-specific UX, favor a stream over ONVIF metadata since the metadata was never load-bearing for this product. **Still not connected to the actual pipeline** (`cloud_pipeline/`, `src/`, `webapp/`) — a confirmed camera produces a working stream URL and nothing downstream of it. That gap is now filed as 6 scoped Linear issues under `Venue Deployment` (`PIC-66`–`71`: capture, portable CFR encode, cloud_pipeline wiring, full-res cut-on-return, CDN delivery, credential scoping) rather than one vague TODO. Full detail in the 09.01 file.

**2026-08-28: `ADR-069`'s open question answered — 720p detection accuracy holds up against real labels**, no degradation found (see `EXPERIMENTS.md`, `progress/08.28...`). One real mistake (wrong model weights file) caught mid-session by sanity-checking against a cached baseline before trusting results. Still open: an uncommitted per-venue calibration (`cloud_pipeline/venues/brickwall-mid-atlantic-720p/calib.json`) from the prior session's ADR-069 work.

**2026-08-27: cloud route now uploads a 720p proxy to R2/RunPod instead of full resolution (`DECISIONS.md` ADR-069).** `calib.json` now records `calibration_resolution`; `pod_infer.py`'s masking and output coordinates are resolution-aware so a lower-res proxy can't silently misalign the court mask (same bug class as `ADR-064`, caught before shipping, not after). Verified byte-identical for old-format calibrations and coordinate-consistent (1.8px mean distance) on a real 720p-vs-native comparison. **Not yet measured: whether detection accuracy holds up at 720p** — only the coordinate mechanism has been verified so far.

**Also 2026-08-27: fixed a real cancel-button race in the web dashboard** (built outside a session, reviewed and one bug found/fixed here) — cancelling before a pod/process was registered previously left the job running unattended, risking a real billed pod nobody was tracking. And: the webapp was found dead mid-job (killed by SIGHUP when its terminal closed, not a crash) — restarted with `nohup` so that can't recur.

**Strategic decisions made 2026-08-26 — read this before re-asking the same questions.** `PIC-47` closed (Linear). Moat hypothesis: **venue relationships** (first-mover + switching cost + footage-flywheel), not the labeled corpus or ranking model — recorded in `STRATEGY.md` §2. Camera angle: **staying behind-baseline**, courtside deprioritized. Delivery architecture: **rolling 10-min chunks preferred over batch** (same total compute, ~8min vs. ~100min delivery latency, spreads GPU load) — caveat: the throughput number behind this is local-GPU-only, never confirmed on RunPod. Movement-analytics business case: **deferred until venue implementation starts.** Trained-classifier direction (`PIC-46`/`PIC-42`): **still not now.**

**Real architecture correction found while scoping the production POC, revised twice same day: `ADR-043`'s direct N100-to-RunPod-serverless upload plan doesn't fit RunPod's real payload limits** (10MB `/run` / 20MB `/runsync`, vs. the plan's ~90MB/hr proxy) — checked against RunPod's actual docs, not assumed. **Fix: land the chunk in storage first. Two real options, both verified against primary docs:** RunPod's own Network Volume (platform feature, `/runpod-volume`, no external-provider integration — confirmed), or **Cloudflare R2, reached directly by the worker's own code via a standard S3 client** (`boto3` + R2's endpoint/`region_name="auto"`, confirmed against Cloudflare's docs — this needs no RunPod-side integration since it's just outbound networking, not a platform feature). R2 is the more attractive option since the same account also covers highlight delivery to end users, which a Network Volume can't do. (First-pass note wrongly said R2 "doesn't work" — that checked the wrong question, whether RunPod's platform integrates with R2, not whether code running on RunPod can reach R2 directly. Corrected after the operator pushed back with a real counter-argument.) Recorded in `DECISIONS.md` (ADR-043 update) and `TECH_SPEC.md` §5.

**Cloud route's GPU pinned to match local (RTX 2000 Ada), 2026-08-26 — operator's call.** Sidesteps a real, never-checked question: every prior inference optimization here (`ADR-064`/`ADR-065`) was verified byte-identical on the *same* GPU, not across different architectures, and the old fallback list (RTX 4090/3090/A4000/3080) meant a rolling-chunk session could land different chunks on different GPU types. `runpod_pod.py`'s `DEFAULT_GPU_TYPES` is now just `["NVIDIA RTX 2000 Ada Generation"]` (confirmed valid + available, $0.24/hr), deliberately no fallback — fails loudly rather than silently trading away the consistency guarantee.

**Pod setup time measured for real, 2026-08-26: ~86s total, not "5-10 min" as earlier guessed.** Pod→SSH 13.1s, TF2.15+deps install 66.5s, weights download+untar 6.6s. Found and fixed two real bugs doing this measurement live: the weights tarball's preserved uid/gid broke extraction as root on a fresh pod (`tar --no-same-owner` fixes it), and `run_cloud_job.py` never loaded `.env` at all (would `KeyError` on any real invocation not manually sourced first — fixed to match `src/verify.py`'s pattern). Changes the custom-image cost/benefit from the earlier conversation — 90s/job is a much weaker case for that engineering cost than several minutes would be.

**`cloud_pipeline` run end-to-end for real, 2026-08-26 — the last untested piece (`pod_infer.py` on a real pod) is no longer untested.** Deliberately fresh test at the operator's insistence: a random 5-minute clip cut from raw, previously-untouched footage, a live calibration the operator clicked through in a real browser (RMSE 0.166ft — calibration requires a human, this agent can't click a browser), then one real `run_cloud_job.py` invocation, watched live. **Exit code 0.** 13 rally segments detected, both reels produced and playable, ranking scores in the normal range. Top clip sent to the operator for a real playback check — mechanical success confirmed, detection quality still pending the operator's own review. One honest number: pod inference measured 29.4fps / ~1.02× wall-clock (essentially real-time) — slower than local's 36.2fps despite the same pinned GPU, not yet root-caused; neither route clears `PRD.md`'s ≤0.5× target. See `DECISIONS.md` ADR-043, `cloud_pipeline/README.md`.

**Calibration corrected twice, 2026-08-26 — second correction reverses the first.** A topology diagram surfaced that `run_cloud_job.py` had no calibration UI; first fix added `ensure_calibration()`, auto-launching `calibrate_web.py` per job. **Operator caught the real mistake: calibration is one-time-per-venue (only changes if the camera physically moves, `ADR-049`), not per-session** — auto-triggering it inside the per-job orchestrator conflated two different cadences. Reverted: `run_cloud_job.py` has zero calibration logic again, `--calib` is required and must pre-exist (`SystemExit` with the fix command if missing). The launch-and-poll mechanism moved to its own one-time script, `cloud_pipeline/setup_venue_calibration.py` — run once per venue, refuses to re-run if a calibration already exists. Both new failure paths verified live. `webapp/app.py`'s local route still calibrates per job in-browser, unchanged — same question applies there, not touched today.

**`cloud_pipeline/` built, 2026-08-26 — a new, isolated R2+RunPod path alongside the existing local-GPU `webapp/`.** Only `webapp/pipeline.py`'s `_run_inference` actually touches local GPU; everything else (drift check, CFR conversion, `build_reel`'s detection/ranking/cutting) is shared, detector-agnostic logic. `cloud_pipeline/` reuses that proven logic and `scripts/pod_infer.py` unmodified (run on a RunPod pod instead of locally) rather than re-implementing anything — isolation means never touching local GPU or `webapp/`, not duplicating already-correct code. Four new files (`r2_storage.py`, `runpod_pod.py`, `pod_r2_helper.py`, `run_cloud_job.py`); nothing existing modified. **Not yet run end-to-end with real GPU inference** — built and import-clean, but the live test (TF2.15 install + real inference on a pod) hasn't happened. See `cloud_pipeline/README.md`.

**Input-side production POC fully validated end-to-end, 2026-08-26 — not just designed, actually tested on real infrastructure.** Credentials saved to `.env` (gitignored). Root cause of an early debugging detour: **Cloudflare doesn't provision the account's S3-compatible endpoint until at least one bucket exists** — a fresh account looks exactly like a broken credential/network problem until one is created. Once fixed, tested from **both ends**: `boto3` `list_buckets`/`put_object`/`get_object` succeeded from the local dev machine, then a real RunPod pod (spun up specifically for this, GPU since CPU-pod capacity was unavailable that moment) confirmed it can reach the same R2 bucket/object directly over its own network — no RunPod-side integration needed, exactly the mechanism discussed. Pod terminated after (~$0.06 total). **What's genuinely left, not started:** compressing/chunking real footage into the proxy format and wiring this into `pod_infer.py` for real, plus the output/delivery leg to end users.

**`PIC-47` (TrackNetV3) is now answered, not open.** On the evidence gathered across three videos, TrackNetV3 with InpaintNet is not a detector worth shipping in place of k14 — it loses on both non-brickwall videos on the metric that matters (`quality:1` recall), because InpaintNet fabricates trajectory on low-raw-detectability footage in a way that generates net crossings not tied to real rallies. Recommended closing the issue as "evaluated, not adopted." The one open, narrower, untested question if this gets revisited: TrackNetV3's raw TrackNet component *without* InpaintNet vs. k14 — not the same claim as the original issue.

**Formal prototype-gate assessment done, 2026-08-26 — `DECISIONS.md` ADR-067, corrected same day by ADR-068. The prototype's actual objective HAS been met.** `PRD.md` §5's numeric targets (recall, FP rate, boundary error, wall clock, utilization) were written into the document's original 2026-07-30 draft, before any real footage/calibration/labels existed — hypothetical placeholders, not calibrated gates (operator correction, ADR-068). The real objective is §2's own question: "is the resulting reel something I actually watch?" — the subjective gate, passed and arguably exceeded (four reels confirmed good by direct playback 2026-08-25, vs. a 3-session minimum). ADR-067's numeric findings (FP rate 2.1–5.3/10min vs. ≤1.0, boundary error 1.2–1.6s vs. ≤1.0s, utilization 0.26–0.82 vs. ≥0.85, wall clock 0.83× vs. ≤0.5×) remain real, correctly-measured, and worth acting on as diagnostics — they trace to one already-known, still-open problem (`PIC-33`/`PIC-55` boundary fragmentation) — but are not gates the prototype needed to clear. `PRD.md` §1's "a production PRD follows" next step is a live decision again, not foreclosed by these numbers. See `feedback_hypothetical_spec_targets` memory for the generalizable lesson.

**`STRATEGY.md` reconciled with the actual architecture, 2026-08-26.** It had a bigger problem than `PRD.md`'s stale numbers: §4/§7/§8 still described a player-position-tracking product that was never built — `ADR-047` (2026-08-13) pivoted the shipped pipeline to ball-crossing detection, and player tracking was deferred before it existed, not after. Rewrote those sections in place: §4 now describes the actual fixed arithmetic pipeline + calibration (not a learned model), §7's "free second product surface" is corrected to a real future build, not an existing byproduct, and §8 now has an honest "what the system actually does today" table (highlights-only) alongside the original hypothetical one. §10's open question #1 corrected — never tested the way it was framed, resolved a different way (`ADR-060`, honest labeling). **Not yet done: the actual strategic decisions** (moat, venue direction, production PRD scope) this reconciliation was meant to make trustworthy ground for — that conversation hasn't happened yet.

**Linear cleanup done 2026-08-25:** 6 issues closed, 5 deprioritized to Low, `PIC-27` built (see below) after being upgraded to High. Full detail in the 08.25 progress file — don't re-review the same issues from scratch next session.

**`track_ball` now returns the tracked x, not just y** (`src/track.py`, `return_x=False` opt-in, `PIC-27` closed) — unblocks `PIC-23`'s corner-angle net-line fix and `PIC-48`'s kitchen-double-crossing trajectory-shape check, neither of which is built yet.

**Full playback review done, 2026-08-25 — operator watched the reels directly and confirmed them good** ("the reels produced are great"). This closes the standing open item on all reels built the prior session: `brickwall_mid_atlantic` (corrected version, `clips/brickwall_mid_atlantic_reel_5min_fixed/`), `IMG_7743` (`clips/IMG_7743_reel_5min/`), `brickwall_pro_series_finals` (`clips/brickwall_pro_series_finals_reel_5min/`), and `IMG_7744` (`clips/IMG_7744_reel_5min/`). Not a per-clip breakdown, just the operator's direct confirmation — good enough to trust the pipeline's output on well-recorded footage, per the operator's own framing. Still open: rally 29's unresolved 3-way conflict on `brickwall-SEMI` (`EXPERIMENTS.md`, 2026-08-23) — narrower, doesn't block anything else.

**Ranking formula (`src/select.py`, ADR-063) is now fully validated** — the combined score separates `quality:1` from `quality:2` on all four graded videos, no exceptions (`scripts/validate_ranking.py`, `EXPERIMENTS.md`/`DECISIONS.md`, 2026-08-24). This was the last explicitly-flagged open item from ADR-063; no weight changes needed.

**Two videos confirmed permanently unusable with the current pipeline** (`EXPERIMENTS.md`, 2026-08-24): `IMG_7652` (continuous non-fixed-camera drift) and `IMG_7655` (camera mounted at the near baseline — the calibration protocol's near-court points are structurally out of frame, likely explaining a 6-week-old "no calibration ever built" gap). Don't re-attempt either without new partial-calibration tooling.

**A real coordinate bug was found and fixed** (`DECISIONS.md` ADR-064) — `scripts/pod_infer.py` scaled TrackNet predictions using one height-derived ratio for both x and y, silently wrong on any non-16:9 video. Found via a diagnostic overlay built for an unrelated reason, fixed, measured exactly (142px max shift, correlating 1.000 with the bug), and confirmed no other video's results need re-checking (only non-16:9 source is affected, and only `brickwall_mid_atlantic` has ever been non-16:9). Committed.

**A confirmed real corner-angle video is in hand** (`Mike-Bill-vs-George-Joshua-at-Rockland.mp4`) but not yet processed — `src/ball.py`'s net-crossing logic needs an angle-aware fix (interpolate net height by image-x, not one flat average) before this footage can be trusted. Fix designed, not built; deferred by the operator.

## Phase gate tracker → [`CHECKLIST.md`](./CHECKLIST.md)

Covers Phase 0–1 only; Linear (`pic-vision` team, `Rally Detection Accuracy` project) is authoritative for current priorities.

## Status at a glance

- **Active detection path: TrackNet** (`src/tracknet.py`, local GPU or RunPod via `scripts/pod_infer.py`). The YOLO path (`src/pipeline.py`, `archive/yolo_pipeline.py`) is retired — ADR-046. TrackNetV3 (a different, newer pretrained architecture) is under evaluation as a possible replacement — see Linear PIC-47 and `progress/08.21 progress overview.md`.
- **Pipeline:** `predictions.csv` → court gate (`src/calib.py`'s `court_wedge`, perspective-aware trapezoid — prefer over the flatter `court_x_range`) → `track_ball` (teleport/re-acquisition confirmation, `src/track.py`) → `crossing_times` → `cluster_crossings` (`src/ball.py`). Shipped default `gap_sec=3.0`, `min_crossings=6` (ADR-048) — **confirmed adequate on all four scored videos with honest labels, 2026-08-23, ADR-060/061.**
- **Built + tested (124 tests):** eval harness (IoU matching at 0.5, detection + selection tables) · calibration (order-independent, browser + CLI, marks the net) · TrackNet prediction parsing (including per-detection blob size/confidence) + court-wedge gating + tracker confirmation · scoring against hand labels on 4 independent camera angles, **all four exhaustively relabeled and re-confirmed 2026-08-23** · adaptive per-video `gap_sec` (PIC-33, opt-in, not yet re-validated against the new labels — `PIC-54`) · a dev-only trained segment classifier (PIC-46, trained on the old incomplete labels — needs retraining, `PIC-54`) · **reel ranking** (`src/select.py`, ADR-063, duration + peak crossing rate + velocity-spike count — see the dedicated section below).
- **Not built:** player-movement quality signals (dives, sprint recoveries — the one selection-signal candidate not yet tested); a boundary-precision fix for `gap_sec` fragmentation and the intentional `start` lead-in offset (`PIC-33`/`PIC-55`); paddle-contact detection (PIC-42, prototyping only). **Selection/ranking is no longer blocked** — see the dedicated section below.
- **Camera-drift detection built** (`src/drift.py`, `scripts/check_drift.py`, PIC-29 closed) — run `check_drift.py` on any new footage *before* calibrating or labelling it.
- **Tried and rejected:** blob size/confidence as a clutter filter (PIC-2) — doesn't separate real balls from junk on this footage. ~~a self-calibrated duration/rate threshold~~ **un-rejected 2026-08-23, ADR-061** — the 2026-08-19 rejection was itself a label-contamination artifact; duration alone is a real signal on honest labels (see below), it's just redundant with the already-shipped `min_crossings=6`.
- **Decided recently:** ADR-046 (TrackNet is the active path), ADR-048 (`min_crossings=6`), ADR-049 (camera bump invalidates calibration), ADR-052 (`eval` role locked on IMG_7743), ADR-053 (label-artefact replacement numbers blocked by adversarial review), ADR-054 (raw crossing *count* isn't a valid shot-count proxy — rate is a separate question, see below), **ADR-057 (IMG_7743 postbump's "12/12 real dead-time crossings" fully retracted — they're missed real rallies), ADR-058/059 (the resulting "recall ceiling" was itself measuring the wrong target — corrected), ADR-060 (capstone: all four videos relabeled, `min_crossings=6` confirmed everywhere, `PIC-31`'s founding premise is nearly moot project-wide), ADR-061 (duration threshold un-rejected), ADR-062 (`LABELING.md` v4 — `start` is an intentional 0–3s viewer lead-in, not literal serve contact; `eval/harness.py` doesn't yet account for it, `PIC-55`).**
- `main` pushed to `origin`, no open branches as of the last `progress/` entry.

## Rally detection — where it stands (updated 2026-08-23)

**Precision is essentially solved.** All four scored videos (IMG_7743 post-bump, IMG_7744, brickwall, pb_draft_cup) were exhaustively relabeled 2026-08-23 after IMG_7743 post-bump's original labels turned out to have missed the large majority of real rallies. Once relabeled: of 30 total residual "false positives" across all four videos, 28 are confirmed real rallies with mismatched boundaries (not detector errors), 2 are unconfirmed either way (`PIC-52`), and **zero are confirmed genuine junk or dead time.** `min_crossings=6` is confirmed adequate on all four videos using the metric that actually matters (recall on `quality:1`/highlight-worthy rallies is flat regardless of the threshold value — lowering it recovers nothing real, only adds noise).

**`PIC-31`'s founding problem — distinguish a real rally from a dead-time/courtesy-tap crossing — is nearly moot on this project's current footage.** There isn't enough confirmed genuine dead time left anywhere in the labeled footage to even test a candidate signal against (`PIC-53`). Don't keep chasing this without new footage that actually contains dead time, correctly distinguished from missed short rallies from the start.

**What's actually left is boundary precision, not detection correctness.** The remaining false positives are timing mismatches, from two compounding, only-partly-disentangled causes: `gap_sec`-clustering fragmentation (`PIC-33`, long-standing) and the newly-codified intentional `start` lead-in (`LABELING.md` v4, `PIC-55`, new). A flat 3s correction for the second cause alone recovered ~1/3 of the mismatches in a quick check — real, but not a full fix.

## Rally start/end boundary signals (2026-08-23)

Distinct from "is this a real rally" above — this is about *when* one begins and ends, still open.

**Start**, candidates in order of how validated they are:
- **First net crossing** — what the pipeline uses today. Precise in principle (a discrete physical event), but the label it's compared against (`start`) is intentionally 0–3s earlier by design (`LABELING.md` v4), not a matching physical event — see `PIC-55`.
- **Pre-serve stillness dip** (near-team ankle-speed drop, `scripts/pose_stillness.py`) — validated 2026-08-23 as a reliable *leading indicator* (88% presence on a 17-rally spread sample across brickwall's full length), but its timing doesn't land on the boundary (median ~1s early, real spread) — useful as a trigger/gate ("watch for a confirming crossing burst next"), not as a timestamp source on its own.
- **Player court-depth-at-serve / post-serve transition timing** — exploratory, one small brickwall sample (2026-08-22), never re-tested at scale.

**End**, much less explored:
- **Last net crossing + `gap_sec` trailing silence** — what the pipeline uses today. Imprecise by construction, always lags the true end by up to `gap_sec`.
- **Post-rally motion spike** (a burst of movement right after the point ends — retrieval, resetting, celebration) — noted once (2026-08-22) as a byproduct observation, opposite polarity from the start-side dip, **never tested at scale** the way the start-side dip was. The clear next parallel piece of work if this direction continues.
- The `end` label itself, unlike `start`, is *not* intentionally offset — `LABELING.md` still defines it as the literal ball-dead moment.

`src/render.py`'s `cut_clips(..., pad_sec=3.0)` already pads real output clips 3s before cutting — this has existed the whole project but is never applied on the scoring path, which is most of why the mismatch above exists (`PIC-55`).

## Rally quality / selection signals (new thread, 2026-08-23)

Previously blocked on precision (now resolved above). First test against real quality grades (`quality:1`/`quality:2`) across all four relabeled videos, three signals validated, each independently consistent in direction on all four:

| signal | how measured | direction |
|---|---|---|
| Duration | rally length | `quality:1` 1.4–1.8x longer |
| Crossing rate | crossings ÷ duration (not raw count — see ADR-054) | consistently higher for `quality:1` |
| Ball velocity | mean of the top-5 fastest ball speeds (duration-normalized — raw peak/max is a duration-count artifact, don't use it) | 8–29% higher for `quality:1` |

Player-movement signals (dives, sprint recoveries — `LABELING.md`'s "skill display" factor) not yet tested — would need the pose-tracking pipeline. Small `quality:1` sample sizes per video (n=3–13) — real, consistent trends, not yet a validated ranking model. Full numbers: `EXPERIMENTS.md`, 2026-08-23 "selection signals" entries.

**A ranking formula combining detector-derived signals is now actually implemented** (`src/select.py`'s `rank_segments`, ADR-063) — `duration` (direct, not a denominator) + `peak_crossing_rate` (max in a 3s sliding window, not the flat average — a flat-rate version was tried first and found to penalize long rallies) + `n_spikes` (raw top-decile ball-speed count). Tuned by live playback review on `brickwall-SEMI`, then validated 2026-08-24 the way the three signals above were: checked against the `quality:1`/`quality:2` grades on all four graded videos, combined score higher for `quality:1` on all four, no exceptions (`scripts/validate_ranking.py`).

## Three failure modes — distinguish these, don't lump them together as "precision"

**Largely historical as of 2026-08-23 — see "Rally detection — where it stands" above for the current picture.** Kept for context on how the project's understanding evolved.

1. **Fragmentation.** A too-tight `gap_sec` splits long rallies and charges the detector twice (a miss plus a false positive). The adaptive `gap_sec` (PIC-33) substantially reduces this on doubles/tournament-length rallies. Still real — now understood as the dominant remaining false-positive mechanism, alongside the `start`-lead-in scoring gap.
2. **Label incompleteness.** Real play that was never marked scores as a false positive — the mechanism behind the entire label-artefact thread (ADR-050/051/053), and then confirmed far more severe than thought on all four videos (ADR-057/058/059/060). Root cause is the old "curate to competitive rallies" habit, which `LABELING.md` warns against. **Fixed 2026-08-23** — all four videos exhaustively relabeled.
3. **Genuine junk**, of two distinct kinds — *phantom crossings* (a tossed ball whose image-y crosses `net_y` without the ball crossing the net) and *courtesy returns* (ball really crosses, but it's dead time). **Turned out to be almost entirely mechanism 2 in disguise** — once labels were fixed, confirmed genuine junk across all four videos dropped to zero (ADR-060).

**Rally length is the hidden variable behind most of this**, and it's driven by format, not camera. Final numbers as of 2026-08-23 (all four videos now relabel-confirmed, not provisional): brickwall is doubles tournament play (21.8s mean rally, 58.1% of the video live); pb_draft_cup is a **singles** match (9.3s, 41.8%); IMG_7743 post-bump and IMG_7744 are casual doubles (37.5% and 24.2% respectively — both far higher than the original ~4–9% figure, which came from the same incomplete labels this whole thread is about). Raw precision is **not comparable across videos with different densities** — a spurious segment lands on real play far more easily when half the video is live. Use the chance-adjusted lift in `EXPERIMENTS.md` when comparing.

## Runnable now (all local — the RTX 2000 Ada does ~28 fps with court masking)

1. Inference env: `/mnt/fast_scratch/tf215_env/venv` (TF 2.15), weights at `/mnt/fast_scratch/tracknet_weights/weights_k14_epoch19`. Export `LD_LIBRARY_PATH` to the venv's `nvidia/*/lib` dirs first.
2. `python3 scripts/pod_infer.py --video game.mp4 --calib calib/<name>_calib.json --output cache/<name>_predictions_k14.csv` — pass `--calib`, it masks outside the court *before* inference.
3. Score: `rally_segments_from_predictions(..., in_court=court_wedge(calib), gap_sec=3.0, min_crossings=6)` → `match_intervals(..., threshold=0.5)`. **Note `match_intervals` returns a dict** (`matches`/`missed`/`false_pos`) — unpacking it as a tuple silently yields string lengths as metrics, which looks plausible and is entirely wrong.
4. **Tests run as `.venv/bin/python -m pytest -q tests/`** — system `python3` has neither pytest nor cv2, and `archive/tests/` fails collection.

## Traps that have cost real time, all now guarded or documented

- **Source `.MOV` files can be corrupt** — IMG_7743/7744 silently decoded 930 of 121k frames, exit 0. Both inference paths now abort below 98%.
- **A calibration without `net_image_points` gives a net line ~130px too low.**
- **A mid-session camera bump invalidates calibration from that instant on** (ADR-049) — now detectable with `scripts/check_drift.py`.
- **IoU≥0.3 vs IoU≥0.5** — numbers logged before 2026-08-17 may use the looser threshold. `TECH_SPEC.md` §11 specifies 0.5.
- **Judge rally-vs-dead-time from playback, never stills or aggregate statistics** — both have produced confidently wrong answers on this project.
- **A Python multiprocessing DataLoader with `num_workers>0` and a large in-memory dataset object can silently hang, not error, if the platform's default start method is `forkserver`/`spawn` rather than `fork`** — each worker gets its own separately-pickled full copy instead of sharing memory. Cost a session on TrackNetV3 (2026-08-21) before being diagnosed; check `multiprocessing.get_start_method()` if a GPU job looks alive but idle.
- **TrackNetV3's `--large_video` streaming loader (`/mnt/fast_scratch/TrackNetV3/dataset.py`) crashes with an `IndexError` if a video's frame count lands exactly on a sliding-window boundary** — the frame buffer empties via the sliding-window slice right at the end, then the padding step indexes `frame_list[-1]` on an empty list. Hit on `pb_draft_cup_30fps.mp4` (624.5s), did not reproduce on `brickwall_30fps.mp4` (1513s) — length-dependent, not universal. Fixed locally (commit `d77102d` in that clone, **not pushed upstream** — it's a third-party repo at `github.com/qaz812345/TrackNetV3`, not part of this git repo or backed up anywhere else). If that scratch clone is ever re-cloned or wiped, this fix needs reapplying: break out of `__iter__`'s outer loop when `frame_list` is empty instead of padding from it.
